   local Rayfield = loadstring(game:HttpGet("https://sirius.menu/gen2"))()

local window = Rayfield:CreateWindow({
    name = "KAISER_VERSION1",
    subtitle = "Created by Lord_Kaiser",
    Theme = "Dark",
})

-- Dynamic Red Glow Border Injector
task.spawn(function()
    task.wait(1)
    local CoreGui = game:GetService("CoreGui")
    local RayfieldGui = CoreGui:FindFirstChild("Rayfield") or CoreGui:FindFirstChild("Sirius")
    
    if RayfieldGui then
        local MainFrame = RayfieldGui:FindFirstChild("Main", true) or RayfieldGui:FindFirstChild("MainFrame", true)
        if MainFrame then
            -- Glowing Red Outline
            local Stroke = Instance.new("UIStroke")
            Stroke.Color = Color3.fromRGB(255, 0, 50) -- Bright Red
            Stroke.Thickness = 3
            Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
            Stroke.Parent = MainFrame

            -- Glow Accent Layer
            local Glow = Instance.new("Frame")
            Glow.Name = "RedGlowBorder"
            Glow.Size = UDim2.new(1, 6, 1, 6)
            Glow.Position = UDim2.new(0, -3, 0, -3)
            Glow.BackgroundColor3 = Color3.fromRGB(255, 0, 50)
            Glow.BackgroundTransparency = 0.6
            Glow.ZIndex = MainFrame.ZIndex - 1
            Glow.Parent = MainFrame

            local GlowCorner = Instance.new("UICorner")
            GlowCorner.CornerRadius = UDim.new(0, 10)
            GlowCorner.Parent = Glow
        end
    end
end)

local tab = window:CreateTab({ name = "Main Features", icon = 93364949241311 })

tab:CreateButton({
    name = "Show Creator User",
    callback = function()
        window:Notify({ title = "KING_KAISER", content = "Username: SUPREME_AURA350." })
    end,
})

tab:CreateToggle({
    name = "Hoverboard RGB",
    callback = function(value)
        print("Hoverboard RGB:", value)
    end,
})

tab:CreateInput({
   name = "Target Player",
   placeholderText = "Type username here...",
   removeTextOnFocus = false,
   callback = function(text)
       _G.TargetPlayer = text
   end,
})

tab:CreateToggle({
   name = "KAISER SPAMMER",
   callback = function(value)
       _G.LoopMessages = value

       task.spawn(function()
           while _G.LoopMessages do
               local TextChatService = game:GetService("TextChatService")
               local ReplicatedStorage = game:GetService("ReplicatedStorage")
               
               local target = (_G.TargetPlayer and _G.TargetPlayer ~= "") and _G.TargetPlayer or "❤️KAISER❤️ H8RS(😝)"

               local messages = {
                   target .. " TMKX MEH DARK POWER KAISER VOID WORM WAVE GOD FACT WAVE PRIME PARALLEL PRIME(🤣)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   target .. " TMKX МЕН PARALLEL DARK KAISER HYPER POWER WAVE PRIME KAISER FACT GODZILLA HYPER(😹)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   target .. " TMKX MEH KAISER WORM PARALLEL GOD WAVE HYPER VOID KAISER WAVE WAVE(😝)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@",
                   target .. " TMKX MEH KAISER WORM PARALLEL LEGEND WAVE HYPER VOID KAISER WAVE POWER(💥)@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
               }

               for _, msg in ipairs(messages) do
                   if not _G.LoopMessages then break end
                   
                   if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                       TextChatService.TextChannels.RBXGeneral:SendAsync(msg)
                   else
                       ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(msg, "All")
                   end

                   task.wait(2)
               end

               task.wait(3)
           end
       end)
   end,
})

task.spawn(function()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer or Players.PlayerAdded:Wait()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")

    -- Custom Name & Bio formatting (Fixed line 111 syntax)
    local customName = '<font color="#00FFFF">💥LORD_KAISER_v1💥</font>'
    local customBio = '<font color="#FF00FF">Welcome ' .. LocalPlayer.Name .. '. ᘜ尺卂几ᗪ Ҝ卂|丂乇尺 ᴏɴ Tᴏᴘ 🤗🔥</font>'

    -- Find Brookhaven's RE Folder
    local re = ReplicatedStorage:WaitForChild("RE", 10)
    if re then
        -- Fire RP Name change
        local nameEvent = re:FindFirstChild("1RPName1") or re:FindFirstChild("RPName")
        if nameEvent then
            nameEvent:FireServer("RolePlayName", customName)
            nameEvent:FireServer(customName)
        end
